# Startora

## 项目简介

Startora 是一个基于 Vue 3 + TypeScript + Vite 构建的现代化个人起始页/导航页应用。它提供了简洁的界面，允许用户自定义添加、编辑和管理常用应用程序快捷方式。后端采用 Node.js (Express) 和 PostgreSQL 提供数据持久化服务，适用于个人起始页或团队常用链接管理场景。

## ✨ 功能特性

- **应用管理**：支持添加、编辑和删除应用程序快捷方式（名称、URL）。
- **数据持久化**：使用 PostgreSQL 存储用户数据和应用配置。
- **现代化 UI**：基于 Naive UI 构建，提供清爽的用户体验。
- **全栈开发体验**：前端使用 Vue 3 生态，后端使用 Express，通过 Concurrently 并行运行。
- **类型安全**：全项目使用 TypeScript（前端）及相关类型定义。

## 🛠 技术栈

### 前端
- **框架**: [Vue 3](https://vuejs.org/) (Script Setup)
- **构建工具**: [Vite](https://vitejs.dev/)
- **语言**: [TypeScript](https://www.typescriptlang.org/)
- **状态管理**: [Pinia](https://pinia.vuejs.org/)
- **UI 组件库**: [Naive UI](https://www.naiveui.com/)
- **HTTP 客户端**: [Axios](https://axios-http.com/)

### 后端
- **运行环境**: [Node.js](https://nodejs.org/)
- **Web 框架**: [Express](https://expressjs.com/)
- **数据库驱动**: [pg](https://node-postgres.com/)

### 数据库
- **数据库**: [PostgreSQL](https://www.postgresql.org/)

## 📂 项目结构

```
startora/
├── src/
│   ├── client/           # 前端源代码
│   │   ├── assets/       # 静态资源
│   │   ├── components/   # Vue 组件 (Login, Main, Config 等)
│   │   ├── store/        # Pinia 状态管理
│   │   ├── types/        # TypeScript 类型定义
│   │   ├── App.vue       # 根组件
│   │   └── main.ts       # 前端入口
│   ├── server/           # 后端源代码
│   │   ├── api.ts        # API 定义
│   │   └── index.cjs     # Express 服务器入口
│   └── db/               # 数据库脚本
│       └── init.sql      # 数据库初始化 SQL
├── .vscode/              # VS Code 配置
├── public/               # 公共静态资源
├── docker-compose.yml    # Docker 编排文件
├── package.json          # 项目依赖与脚本
├── vite.config.ts        # Vite 配置文件
└── ...
```

## 🚀 快速开始

### 环境要求
- Node.js (推荐 v16+)
- PostgreSQL (或使用 Docker)
- pnpm / npm / yarn

### 1. 安装依赖

```bash
# 推荐使用 pnpm
pnpm install
# 或者 npm
npm install
```

### 2. 数据库配置

确保本地安装了 PostgreSQL，并创建一个名为 `startora` 的数据库。
如果你有 homebrew，你可以使用以下命令安装 PostgreSQL 15：
```bash
brew install postgresql@15

```
然后运行初始化脚本：

```bash
# 登录 PostgreSQL
psql -U postgres

# 执行初始化脚本 (请根据实际路径调整)
\i src/db/init.sql
```

> **注意**：后端连接配置位于 `src/server/index.cjs` 中，默认配置为：
> - User: `postgres`
> - Password: 请在本地配置
> - Host: `localhost`
> - Port: `5432`
>
> 请根据你的本地环境修改该文件中的数据库连接配置。

### 3. 启动开发服务器

该命令会同时启动前端 Vite 开发服务器和后端 Express 服务器：

```bash
npm run dev
```

启动后，访问 `http://localhost:5173` 即可查看应用。

### 4. 构建生产版本

```bash
npm run build
```

## 🧩 使用示例

1. 启动开发服务器后，打开 `http://localhost:5173`
2. 在页面输入 App Name 与 App URL
3. 点击 add app 新增快捷方式
4. 点击卡片右上角编辑按钮更新名称或 URL

## 🔑 关键入口与配置

- 前端入口：`src/client/main.ts`
- 前端根组件：`src/client/App.vue`
- 后端入口：`src/server/index.cjs`
- API 访问基址：`http://localhost:3000`
- 构建配置：`vite.config.ts`

## 📖 目录说明

- **`src/client`**: 包含所有前端逻辑。`components/main.vue` 是主要的应用展示区，`components/config.vue` 负责配置界面。
- **`src/server`**: 简单的 Express 服务器，提供 RESTful API 以供前端调用，处理用户和应用数据的 CRUD 操作。
- **`src/db`**: 包含数据库架构定义的 SQL 文件。

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request 来改进本项目。

1. Fork 本仓库
2. 创建你的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交你的修改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启一个 Pull Request

## 📄 许可证

[待补充]
